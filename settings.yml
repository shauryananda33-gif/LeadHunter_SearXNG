use_default_settings: true

general:
  debug: false
  instance_name: "LeadHunter Search"

search:
  safe_search: 0
  formats:
    - html
    - json

server:
  secret_key: "ultrasecretkey"
  limiter: false
  public_instance: false
  image_proxy: false
  method: "GET"
